name: build worflow

on:
  push:
    branches:
      - main
      - feature/**

jobs:
  build_pipeline:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    name: build job
    runs-on: ${{ matrix.os }}
    environment: deployment
    defaults:
      run:
        working-directory: ./
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - name: installing dependencies
        run: npm ci
      - run: echo "${{ secrets.SECRETS }}"
      - name: adding .env
        run: echo "${{ secrets.SECRETS }}" > .env.local
      - name: build step
        run: |
          ls -a
          npm run build
